export const REST_API_BASE_URL = import.meta.env.VITE_REST_API

export const ENVIRONMENT = import.meta.env.VITE_ENVIRONMENT == null ? "PRODUCTION" : "DEBUG"

// Define some constants will use 
export const BASE_URL = '/frontend';